{
  "name": "note-backend",
  "version": "1.0.0",
  "main": "server.js",

  "scripts": {
    "start": "node server.js",
    "build": "prisma generate",
    "deploy": "npm run build && node server.js"
  }
}
,
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "@prisma/client": "^4.0.0"
  }
}